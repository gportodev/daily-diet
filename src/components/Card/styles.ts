import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  iconStyle: {
    top: 8,
    right: 8,
    alignSelf: 'flex-end',
    position: 'absolute',
  },
});
